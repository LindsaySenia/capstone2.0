const options = require('./odb.json');

const sortingBtn = document.getElementById('hat');





sortingBtn.addEventListener('click', e => {
    e.preventDefault();

    console.log('sorting away!')
})